<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bootreceiver.app">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" 
        tools:ignore="QueryAllPackagesPermission" />
    <!-- Permissão para abrir apps mesmo com tela bloqueada (Android 10+) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Permissão para reiniciar dispositivo (requer Device Admin) -->
    <uses-permission android:name="android.permission.REBOOT" />

    <application
        android:name=".BootReceiverApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BootReceiver"
        android:usesCleartextTraffic="true">
        
        <!-- Activity principal para seleção de app (primeira vez) -->
        <activity
            android:name=".ui.AppSelectionActivity"
            android:exported="true"
            android:theme="@style/Theme.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BroadcastReceiver para escutar o boot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Serviço para verificar internet e abrir app -->
        <service
            android:name=".service.BootService"
            android:enabled="true"
            android:exported="false" />

        <!-- Serviço para monitorar comandos de reiniciar do Supabase -->
        <service
            android:name=".service.RebootMonitorService"
            android:enabled="true"
            android:exported="false" />

        <!-- Device Admin Receiver para permitir reiniciar dispositivo -->
        <receiver
            android:name=".receiver.DeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
