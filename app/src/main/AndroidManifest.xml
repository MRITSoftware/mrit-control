<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" 
        tools:ignore="QueryAllPackagesPermission" />
    <!-- Permissão para abrir apps mesmo com tela bloqueada (Android 10+) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Permissão para finalizar processos em background (reiniciar app) -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <!-- Permissão para Foreground Service (Android 9.0+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".BootReceiverApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BootReceiver"
        android:usesCleartextTraffic="true">
        
        <!-- Activity principal para seleção de app (primeira vez) -->
        <activity
            android:name=".ui.AppSelectionActivity"
            android:exported="true"
            android:theme="@style/Theme.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity de status e debug -->
        <activity
            android:name=".ui.StatusActivity"
            android:exported="false"
            android:theme="@style/Theme.BootReceiver" />

        <!-- BroadcastReceiver para escutar o boot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Serviço para verificar internet e abrir app -->
        <service
            android:name=".service.BootService"
            android:enabled="true"
            android:exported="false" />

        <!-- Serviço para monitorar comandos de reiniciar app do Supabase -->
        <service
            android:name=".service.AppRestartMonitorService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
